apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "custom-metrics-apiserver.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "custom-metrics-apiserver.name" . }}
    helm.sh/chart: {{ include "custom-metrics-apiserver.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "custom-metrics-apiserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
